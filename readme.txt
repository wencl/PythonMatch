第一种情况：数据为0的情况
1.首先找出没有停牌的股票
2.没停牌的股票中找出时间点为0的错误数据
3.找出时间不为0但数据值为0的错误数据(指数要分开)

第二种情况：浮动比较大的情况 (可以调整待定)
1.俩点之间浮动超过2%的股票

找错误数据->生成可视化图像(名字为空的不可视化)->发送邮件->查看

    没有停牌的股票(判断条件:名字不为空的股票) 全局变量

        传入一个开盘时间点查出数据
            找出名字为空的股票

            和没有停牌的股票对比 查出时间点为空的股票

            有时间点

                指数 单独拿出来处理 指数没有均价

                股票 均价或者当前价为空的股票

        找出波动比较大的股票 取前一分的数据 对比 找出涨跌幅 大于2%的股票

    用字典dict来存储这些不同类型的错误数据。


二、用到的模块:
 pymongo 链接数据库

 pandas 数据处理

 SMTP是发送邮件的协议，Python内置对SMTP的支持，可以发送纯文本邮件、HTML邮件以及带附件的邮件。
 Python对SMTP支持有smtplib和email两个模块，email负责构造邮件，smtplib负责发送邮件。

 pyecharts 数据可视化

 apscheduler 定时任务